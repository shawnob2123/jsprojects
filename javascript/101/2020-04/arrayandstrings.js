let numbers = ["1", "2", "3", "4", "5"];


let total;


for (let i = 0; i < numbers.length; i++) {
     total = total + parseInt(numbers[i], 10);
   
}

console.log(total);

//create a program to add odd numbers
// make it work inside of a function
// work on FCC basic JS in VS code