// object literals

let user = {
    name: 'crystal', //name = key | crystal = value
    age: 30,
    location: 'Rhode Island',
    email: 'crystalkills@yahoo.net'

};

console.log(user.name); //key is accessed by dot notation
console.log(user['name']); //key is accessed by bracket notation. MUST BE IN A STRING